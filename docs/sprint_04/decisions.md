# Sprint 04 Decisions and Deviations

Record sprint-specific decisions and deviations. Cross-sprint decisions belong in ADRs.

## 2026-08-08 — PBI-04-01: Extended the existing `azure-pipelines.yml` in place, not a new parallel pipeline file

**Decision:** PBI-00-07 (Sprint 0) already created a working `azure-pipelines.yml` with 5 stages (BackendQuality, FrontendQuality, InfrastructureValidation, ContainerBuildValidation, ArtifactPublication) plus a documented, `condition: false`-disabled `Deploy_Dev` stage describing the intended future CD flow in detail. This PBI edits that same file in place — fixing two real, pre-existing defects it contained, then adding the new build/push/deploy/smoke-test/summary stages — rather than creating a second, competing pipeline file.

**Deviation/status change:** None — this is the only sane approach given a working CI foundation already exists; duplicating it would create two sources of truth for the same quality gates and violate CLAUDE.md §7's "make the smallest viable change."

**How to apply:** Any future pipeline stage (a QA/Prod deploy, once those environments are explicitly authorized) should be added to this same file, following the same `dependsOn`/`condition` gating pattern already established, not a new file.

## 2026-08-08 — PBI-04-01: Two real, pre-existing defects found and fixed in the inherited pipeline

**Decision:** Reading the existing `azure-pipelines.yml` before changing it (per this PBI's own explicit "Read first" instruction) surfaced two genuine bugs, neither introduced by this PBI:

1. **Wrong Docker build context for the API image.** `ContainerBuildValidation`'s original step ran `docker build -t "$(apiImageName):$(imageTag)" ./apps/api`. `apps/api/Dockerfile`'s own header comment has required the **repo root** as the build context since PBI-03-02, because `main.py` depends on the shared, reusable `src/` packages (`src.supervisor`, `src.agents`, `src.domain`, `src.services`) which Docker cannot `COPY` from outside the build context — a single-directory context can never reach both trees. This command would have failed at the first `COPY src ./src` instruction had it ever actually been run with a real registry push attached (Sprint 0's version never pushed anywhere, so the bug was silent — the stage only proved a *local, throwaway* image tag could be *attempted*, and apparently was never actually exercised end-to-end against the post-PBI-03-02 Dockerfile). Fixed: `docker build --file apps/api/Dockerfile .` (repo root context), matching `docker-compose.yml`'s own `context: .` / `dockerfile: apps/api/Dockerfile` pattern and every `az acr build` invocation from PBI-03-05/06's real deployment validation.
2. **Stale Bicep module validation list.** `InfrastructureValidation`'s `bicepModuleFiles` parameter listed only 10 of the 14 files under `ops/bicep/modules/` — `ai-search.bicep` (PBI-02-02), `azure-openai.bicep` (PBI-03-02), and `private-dns-zone.bicep`/`private-endpoint.bicep`/`virtual-network.bicep` (PBI-03-04) were added by later PBIs but never added to this list, meaning the pipeline never actually validated them. Fixed: the list now matches `ls ops/bicep/modules/*.bicep` exactly (verified), 14 modules + `main.bicep`.

**Deviation/status change:** Both are corrections of latent, previously-undetected defects — not scope creep. Fixing them was necessary for this PBI's own stated objective ("Automatic Docker build" and "Automatic push to existing ACR" cannot work at all with the first bug present) and for "no regression" (the second bug meant Sprint-0's own stated CI coverage was silently incomplete).

**How to apply:** Any future new Bicep module must be added to this same `bicepModuleFiles` list as part of the PBI that adds it — this was the exact gap that let the list go stale for three PBIs running. Consider this list a checklist to update whenever `ops/bicep/modules/` gains a new file.

## 2026-08-08 — PBI-04-01: Reused the existing Managed Identity for the Azure DevOps service connection, via Workload Identity Federation — no new identity, no stored secret

**Decision:** The new Azure DevOps service connection (name: `tmx-agent-platform-dev-oidc`, parameterized in `azure-pipelines.yml`, not hardcoded inline) is designed to target the platform's **existing** user-assigned Managed Identity (`id-tmxap-dev`), via Workload Identity Federation (OIDC) — exactly the pattern `docs/sprint_00/security-baseline.md` §6 already recommended when PBI-00-07 first built the CI foundation, and never implemented until now. No new identity, service principal, or client secret was created. That same identity now needs two additional roles it didn't have before:

- **AcrPush** on the existing Container Registry (`ops/bicep/modules/container-registry.bicep`, new `pushPrincipalId` param, mirroring the existing `pullPrincipalId`/AcrPull pattern exactly) — role GUID `8311e382-0749-4cb8-b61a-304f252e45ec`, confirmed against the live Azure role-definition catalog via `az role definition list --name AcrPush`, not guessed.
- **Container Apps Contributor**, scoped to *each Container App individually* (`ops/bicep/modules/container-app.bicep`, new `cicdPrincipalId` param), not the resource group — role GUID `358470bc-b998-42bd-ab17-a7e34c199c0f`, likewise confirmed live. Its own permission set (`Microsoft.App/containerApps/*`, `Microsoft.App/managedEnvironments/*/read`, `.../join/action`) is inherently scoped to Container Apps resources regardless of assignment scope, so per-resource assignment here is a genuine least-privilege choice, not just a formality: this identity still cannot touch Cosmos DB, Key Vault, Azure OpenAI, AI Search, the registry's own management plane, or the *other* Container App via this role.

Both new role-assignment resources are wired unconditionally in `main.bicep` (same pattern as the four existing data-plane role assignments), reusing `managedIdentity.outputs.principalId` — the identical principal already granted Cognitive Services OpenAI User, Search Index Data Reader, Cosmos DB Data Contributor, Key Vault Secrets User, and AcrPull.

**Deviation/status change:** A genuinely new capability grant (this identity previously had zero control-plane RBAC), but explicitly authorized and required by this PBI's own instructions ("Use secure service connections and managed identities where appropriate... DO NOT create additional Azure resources unless absolutely required by Azure DevOps itself" — a role assignment enabling the pipeline to function at all is exactly that exception). **Not yet applied to real Azure**: validated only via `az bicep build` (exit 0, 0 warnings on `main.bicep`, `container-registry.bicep`, `container-app.bicep`, and all three `.bicepparam` files) this PBI. Applying it requires `az deployment group create` against the existing `rg-tmx-agent-platform-dev` — a live Azure mutation this PBI's own "Validation" section does not list (unlike PBI-03-05/06, which explicitly required live Azure validation), so it is deliberately deferred pending explicit user approval, per CLAUDE.md §7/§17.

**How to apply:** Before the pipeline's `ContainerBuildAndPush`/`DeployDev` stages can succeed for real, someone with Owner/User Access Administrator on the subscription (the user already has this — confirmed in PBI-03-05) must: (1) run `az deployment group create --resource-group rg-tmx-agent-platform-dev --template-file ops/bicep/main.bicep --parameters ops/bicep/parameters/dev.bicepparam` to apply the two new role assignments (idempotent — every other resource is already byte-identical to its current deployed state, so this redeploy only adds the two new role assignments, changing nothing else); (2) create the Azure DevOps service connection itself (Azure DevOps portal → Project Settings → Service connections → New → Azure Resource Manager → Workload identity federation → target the existing `id-tmxap-dev` identity) — this second step is an Azure DevOps organization-level action no CLI session without an Azure DevOps PAT/org URL can perform, and is therefore a genuine, documented manual prerequisite, not an oversight.

## 2026-08-08 — PBI-04-01: Container Build and Push Images combined into one YAML stage, not two

**Decision:** The user's own instructions list "Stage 4 — Container Build" and "Stage 5 — Push Images" as distinct numbered items. `azure-pipelines.yml` implements both inside a single YAML `stage: ContainerBuildAndPush`, as two separate, clearly-labeled `docker build`/`docker push` command groups within one job, rather than as two separate top-level `stage:` blocks.

**Deviation/status change:** A deliberate technical choice, not a shortcut: Azure Pipelines stages do not share a filesystem or Docker image cache across stage boundaries (each stage can run on a different agent instance) — a genuinely separate "Push Images" stage would need to `docker save` the just-built image, publish it as a multi-hundred-MB pipeline artifact, download it in the next stage, and `docker load` it, purely to satisfy a stage-per-numbered-item mapping. For a DEV-only pipeline with no manual approval gate between build and push (nothing would ever pause a real deployment *between* these two steps), that round-trip adds pipeline minutes and complexity with zero corresponding benefit — a violation of CLAUDE.md §7's "avoid unnecessary complexity." The two responsibilities remain fully visible and independently reviewable as separate `docker build`/`docker push` commands with their own `echo` markers in the step's inline script and this stage's own header comment.

**How to apply:** If a future PBI adds a manual approval gate or a security/vulnerability scan that must run *between* build and push (a real reason to need the artifact roundtrip), split them into two stages at that point — not before, since no such requirement exists today.

## 2026-08-08 — PBI-04-01: Deploy-affecting stages gated to the `main` branch only; routine `develop`/PR runs stop after quality gates, exactly as before

**Decision:** `ContainerBuildAndPush`, `DeployDev`, `SmokeTests`, and `DeploymentSummary` all carry `condition: and(succeeded(), eq(variables.isDeployRun, true))`, where `isDeployRun` compares `Build.SourceBranch` against a new `deployBranch` parameter (default `'main'`). Pushes to `develop` and pull-request validation builds still run `BackendQuality`/`FrontendQuality`/`InfrastructureValidation`/`ArtifactPublication` exactly as they did before this PBI — identical behavior to Sprint 0 — but never reach the new deploy stages.

**Deviation/status change:** None — a direct implementation of this PBI's own "automatically deploys ONLY to the existing DEV environment" objective, interpreted as "on integrated/reviewed changes reaching `main`," not "on every feature-branch push," which would be noisy and wasteful for a project with no branch-protection/PR-gate details specified elsewhere in CLAUDE.md.

**How to apply:** If this project's actual branching model differs (e.g., `develop` is the integration branch that should deploy to DEV, not `main`), change the single `deployBranch` parameter default at the top of `azure-pipelines.yml` — no other line needs to change.

## 2026-08-08 — PBI-04-01: Image tag versioning (`dev-$(Build.BuildId)`) structurally avoids a bug discovered during PBI-03-05/06's manual validation

**Decision:** Every pipeline run tags both images `dev-$(Build.BuildId)` — Azure DevOps's own unique, monotonically increasing build identifier, prefixed for readability. Never `latest`, and never a fixed placeholder tag reused across runs.

**Deviation/status change:** None — directly required by this PBI's own "Use image versioning instead of latest whenever possible" instruction. Documented here because it also happens to structurally prevent a real bug PBI-03-05/06 hit manually: `az containerapp update --image <same-string-as-before>` is a no-op in Azure Container Apps (the platform compares the image string, not the resolved digest) — PBI-03-05/06 discovered this when reusing the placeholder tag `pending-first-build` across multiple manual redeploys and had to add `--revision-suffix` to force a new revision each time. Because every pipeline run's tag is genuinely unique, `az containerapp update --image ...` in `DeployDev` will always see a changed image string and always create a new revision correctly — no `--revision-suffix` workaround is needed or used.

**How to apply:** Do not introduce a shared/reused tag (e.g., `dev-latest`) into the routine pipeline flow — it would silently reintroduce the exact no-op-update bug this design already avoids.

## 2026-08-08 — PBI-04-01: Pipeline validation was static/offline only — no real Azure DevOps organization access, no live Azure mutation, no live pipeline run this PBI

**Decision:** This PBI's own "Validation" section lists `pytest`, `ruff`, `mypy`, "Azure Pipeline YAML validation", and "Local dry-run where possible" — deliberately not including a live Azure deployment or an actual triggered pipeline run (unlike PBI-03-05/06, whose STOP CONDITIONs explicitly required live Azure validation). Consistent with that scope: `azure-pipelines.yml` was validated via Python `yaml.safe_load` (confirms syntax, all 8 stages parse), a full manual schema/logic review against the working Sprint-0 baseline pipeline, and real, read-only `az` CLI dry-runs of every dynamic-resolution query (`az acr list`, `az containerapp list`/`show`) and both smoke-test commands (`curl /health`, `curl -X POST /chat` plus the exact Python JSON-assertion snippet used in the pipeline) against the actual live DEV environment — all confirmed correct against real data, not assumed. No `az devops` CLI extension is installed and no Azure DevOps organization/PAT is configured in this session (confirmed: `az devops configure -l` fails with no org context), so a true schema-level Azure Pipelines validation (`az pipelines run --validate-only` or equivalent) and an actual triggered run could not be performed.

**Deviation/status change:** None — an honest scope boundary, not an oversight, consistent with CLAUDE.md's "never claim a feature... succeeded unless it was actually executed." The two things NOT done (applying the RBAC Bicep change to real Azure; creating the Azure DevOps service connection and running the pipeline for real) are both explicitly listed as pending, user-approval-gated next steps in this file and in `README.md`'s "Out of scope" section, not silently omitted.

**How to apply:** The next PBI (or the user directly) that wants to actually exercise this pipeline must, in order: (1) approve and run the `az deployment group create` for the RBAC additions (see the Workload Identity Federation decision above); (2) create the Azure DevOps service connection; (3) push to `main` (or trigger the pipeline manually) and observe the real run. Any failure at that point should be diagnosed against this file's documented design — e.g., a permission error on `az acr login`/`docker push` most likely means step (1) wasn't done yet, not a pipeline YAML defect.

## 2026-08-08 — PBI-04-01: RBAC additions applied to the real DEV environment, after explicit `what-if` review and explicit user approval

**Decision:** Per explicit user instruction, the two new role assignments documented above (AcrPush on the ACR; Container Apps Contributor scoped individually to each Container App) were applied to the real `rg-tmx-agent-platform-dev` via `az deployment group create --resource-group rg-tmx-agent-platform-dev --template-file ops/bicep/main.bicep --parameters ops/bicep/parameters/dev.bicepparam` — but only after: (1) `az deployment group what-if` was run and its full output reviewed; (2) the output was confirmed to show zero resource creates, zero deletes, and zero replacements at the top resource level — every `~ Modify` entry was either read-only/computed-property "what-if noise" (the same class already documented in `docs/sprint_03/decisions.md` from PBI-03-05's own what-if runs) or a `reference()`-expression rendering of a value that already resolves identically to what is deployed; the 8 "Unsupported" diagnostics were all `Microsoft.Authorization/roleAssignments` extension resources (a documented ARM what-if limitation for `guid()`-named resources depending on a live `reference()` — see `aka.ms/WhatIfUnidentifiableResource`), 5 of which were the pre-existing role assignments from PBI-03-02/03-04 and 3 of which were this change's own new roles; (3) this change summary was shown to the user; (4) explicit approval ("Approved. Proceed with az deployment group create.") was received before running the mutating command.

The deployment reached `provisioningState: Succeeded`. Post-deployment verification (all read-only `az` calls):
- `az role assignment list --assignee <id-tmxap-dev's principalId> --all` confirms all 7 role assignments now held by the identity: the 4 pre-existing Azure-RBAC roles (AcrPull, Key Vault Secrets User, Search Index Data Reader, Cognitive Services OpenAI User — Cosmos DB Data Contributor is a separate, Cosmos-native SQL role assignment not visible via this command, and was not touched by this change) plus the 3 new ones this PBI added: AcrPush on `acrtmxapdevl3fgxt`, and Container Apps Contributor on both `ca-tmxap-dev-api` and `ca-tmxap-dev-web` individually.
- `az resource list --resource-group rg-tmx-agent-platform-dev` confirms all 12 resources still present, all `Succeeded`.
- Both Container Apps' active revision names are **unchanged** from before this deployment (`ca-tmxap-dev-api--v5`, `ca-tmxap-dev-web--phnq58i`) — direct, positive confirmation that this deployment did not create a new revision, redeploy the running containers, or otherwise touch application state; only the new extension (role-assignment) resources were added.
- Both revisions report `healthState: Healthy`.
- `GET /health` returns `200 {"status":"ok"}`.

**Deviation/status change:** None — this is the planned, previously-deferred follow-up to the "Workload Identity Federation" decision above, executed only once explicitly authorized, exactly as that entry's own "How to apply" section anticipated.

**How to apply:** The Managed Identity now has everything Bicep-side it needs to push images and update the two Container Apps. Per the same user instruction, the Azure DevOps pipeline itself was deliberately **not** triggered in this step — creating the Azure DevOps service connection (an org-level, human, one-time action outside this session's reach) remains the next, separate prerequisite before any real pipeline run can be attempted; see this file's earlier "How to apply" note for the full remaining sequence.

## 2026-08-08 — PBI-04-02: CORS made configuration-driven, never "*", origin resolved dynamically from the existing Web Container App

**Decision:** `apps/api/src/config/settings.py` gained `cors_allowed_origins: str = "http://localhost:3000"` (comma-separated, matching local docker-compose's default Web port) plus a `cors_allowed_origins_list` property. `apps/api/src/main.py` wires `CORSMiddleware` — added *before* `CorrelationIdMiddleware` so it is the outermost layer (Starlette wraps middleware in add-order, first-added = outermost), `allow_origins=settings.cors_allowed_origins_list`, `allow_credentials=False` (this API uses no cookies/session auth), `allow_methods=["GET", "POST"]` (the only methods this API exposes), `allow_headers=["Content-Type", "X-Correlation-ID"]`. `ops/bicep/main.bicep` sets `CORS_ALLOWED_ORIGINS` on the API Container App to `'https://${webContainerApp.outputs.fqdn}'` — the deployed Web Container App's own FQDN, resolved dynamically through Bicep's own module-output wiring, never a hardcoded domain string. `"*"` is never used anywhere in this configuration, satisfying the explicit requirement for the production-shaped DEV posture.

**Deviation/status change:** A genuinely new capability (the API had zero CORS handling before this PBI — confirmed via a real live `curl` test with an `Origin` header returning no `Access-Control-Allow-Origin` header prior to this fix), required because the Web app and API app are different Container Apps with different origins by construction (no reverse proxy/same-origin trick exists in this architecture). Not a redesign of any existing behavior — every other route/response is unchanged; 6 new tests (`tests/unit/api/test_cors.py`) confirm both the allow-list parsing and the real FastAPI app's CORS behavior (preflight, actual request, and the negative case: an unlisted origin gets no CORS header).

**How to apply:** Any future environment (staging/prod) automatically gets the correct origin the same way — `main.bicep`'s reference to `webContainerApp.outputs.fqdn` requires no per-environment CORS configuration. If a second real caller origin is ever needed (e.g., a future mobile app or a custom domain in front of the Web Container App), extend `CORS_ALLOWED_ORIGINS` as a comma-separated list — the parsing already supports multiple origins; only the Bicep env var value would need a second entry appended.

## 2026-08-08 — PBI-04-02: Critical, pre-existing `ToolCallingOrchestrator` defect discovered during live validation — NOT fixed, out of this PBI's authorized scope

**Decision:** Live multi-turn validation against the real Azure OpenAI (`gpt-5-mini`) deployment surfaced a genuine, previously-undetected defect: `src/core/tool_calling/orchestrator.py::run()` (lines ~113-122), after executing an LLM-requested tool call, appends only a `role="tool"` `LLMMessage` to the conversation before looping back for a follow-up LLM call — it never appends the preceding `role="assistant"` message containing the `tool_calls` request itself. The real Azure OpenAI API rejects this exact malformed sequence: `"Invalid parameter: messages with role 'tool' must be a response to a preceding message with 'tool_calls'."` (HTTP 400, surfaced to the client as a `500 Internal Server Error` via the existing `LLMProviderError` path). `MockLLMProvider` — the only provider every existing automated test exercises this loop against — never validates OpenAI's real message-sequencing constraint, so this defect was invisible through PBI-01 through PBI-03-06's entire test suite and every prior manual smoke test (none of which happened to need a *second* LLM round-trip after a tool call against the real API).

**Scope of impact, precisely determined via live testing, not assumed:** Only `ClaimsAgent` is wired to `ToolCallingOrchestrator` (confirmed in `apps/api/src/api/dependencies.py`'s `get_supervisor()` — `BrokerAgent`/`CommercialIntakeAgent` receive no `tool_calling_orchestrator` argument at all). `BrokerAgent` and `CommercialIntakeAgent` invoke Tools through a separate, deterministic path (not LLM-driven function-calling), confirmed unaffected by live multi-turn tests (both completed a full 2-turn exchange, including real Tool-sourced business data, with `200` responses). `ClaimsAgent`'s first turn (routing + LLM response + RAG grounding + citations, no tool call yet) also succeeds live. The defect fires specifically the moment the LLM decides to request *any* tool call within a Claims conversation and the orchestrator loops back for the tool result to be incorporated — which is expected to happen in essentially every realistic, complete Claims intake conversation (policy validation, claim registration), making full multi-turn Claims currently broken against the real Azure OpenAI deployment.

**Deviation/status change:** Discovered, not caused, by this PBI — no source change in `src/agents/`, `src/core/tool_calling/`, or `src/llm/` was made this PBI (confirmed by `git status`/`git diff`: only `apps/api/src/{main.py,config/settings.py}`, `apps/web/**`, `ops/bicep/main.bicep`'s `CORS_ALLOWED_ORIGINS` line, and this documentation were touched). Per this PBI's own explicit instruction ("Do not modify Agents, Supervisor, Tools, RAG, PromptManager, LLMProvider, Cosmos, or Azure infrastructure"), `orchestrator.py` was correctly left unmodified even though the fix is well-understood and precise (insert an `assistant` `LLMMessage` carrying the model's own `tool_calls` before the `tool`-role result message(s), mirroring the OpenAI/Azure OpenAI chat-completions contract exactly).

**How to apply:** This is an urgent, precisely-scoped, high-value follow-up PBI — fix `ToolCallingOrchestrator.run()` to append the assistant's own tool-calling message to `conversation` before the tool result message(s), add a unit test asserting the resulting message sequence is well-formed (assistant-with-tool_calls immediately followed by matching tool-role messages), and re-validate a full multi-turn Claims conversation against the real Azure OpenAI deployment end-to-end. Until fixed, this PBI's live validation documents Claims as working end-to-end for a single turn only (including real grounded RAG citations) — see `validation.md` for the exact evidence and the honest STOP CONDITION accounting.
