# Sprint 02 Validation

Record only commands actually executed and their real results.

## 2026-08-07 — PBI-02-01: Knowledge / RAG Framework

| Command | Result |
|---|---|
| Branch topology check (`git log`, `git rev-parse main HEAD`) | Confirmed `feat/pbi-02-01-knowledge-framework` was cut from the already-merged PBI-01-07 tip (`583731b`), same commit as `main` — no merge needed |
| `pytest tests/unit/prompts -v` (immediately after the additive `PromptRenderContext`/renderer change, before writing any RAG code) | `24 passed` — confirms the additive `retrieved_knowledge` field/`{retrievedKnowledge}` placeholder is fully backward-compatible before building on top of it |
| `pytest tests/unit/rag tests/unit/agents/test_claims_agent_knowledge_integration.py -v` (new tests only) | `27 passed` — clean on the first run |
| `pytest tests/unit/agents -v` (full agent regression after wiring `knowledge_retriever` into `ClaimsAgent`'s constructor) | Attempt 1: `115 passed, 1 failed` — `test_agent_response_is_stable_when_input_has_no_recognizable_claims_fields` failed because its two probe messages ("first message" / "completely different message") did not, in fact, have equal keyword overlap with the new knowledge base (the word "different" matched `KB-COMMERCIAL-0001`) — a genuine, expected new source of input-dependence, same class of change as PBI-01-04's documented LLM-input-dependence fix. Corrected the test's probe messages to two words guaranteed to have zero overlap with the knowledge base. Attempt 2: `116 passed` |
| `pytest tests/unit/api tests/unit/domain tests/unit/services tests/unit/supervisor tests/unit/agents tests/unit/tools tests/unit/prompts tests/unit/llm tests/unit/rag tests/integration -v` (full suite) | Attempt 1: collection error (`test_models.py` basename collision between `tests/unit/rag/` and `tests/unit/llm/`, same recurring class of issue as every prior PBI since PBI-00-02). Fixed by renaming to `test_rag_models.py`. Attempt 2: `304 passed, 2 skipped` |
| `ruff check apps/api/src src tests` | `All checks passed!` — clean on the first run |
| `mypy apps/api/src` / `mypy src` | Both clean (13 and 83 files) |
| `mypy tests/unit/llm tests/unit/agents tests/unit/tools tests/unit/prompts tests/unit/supervisor tests/unit/services tests/unit/rag` | Clean (47 files). `tests/unit/api/*` excluded for the same pre-existing, unrelated reason as every prior PBI |
| `git status` after building `data/raw/knowledge_base/` | The 5 new documents did not appear as untracked — `.gitignore`'s `data/raw/*` rule ("Sensitive or real business data") was silently swallowing them, the same class of issue as PBI-01-02's `secrets/` collision. Relocated to `configs/knowledge_base/` (not gitignored, and the better semantic fit — curated/versioned reference content, mirroring `configs/prompts/`) rather than editing the security-relevant ignore rule. Updated all 9 referencing files (Dockerfile, `dependencies.py`, 7 test files) |
| `pytest`/`ruff`/`mypy` re-run after the relocation | `304 passed, 2 skipped`; ruff and mypy both clean — unaffected by the relocation |
| `docker compose config` (temporary local `.env` copied from `.env.example`, removed after) | exit 0 |
| Docker packaging review | `apps/api/Dockerfile` did not copy any knowledge-base directory into the image — same class of gap as PBI-01-03's `configs/prompts/` omission. Fixed by adding `COPY configs/knowledge_base ./configs/knowledge_base`, following the exact precedent |
| `docker build` | NOT executed — Docker daemon unavailable locally (recurring environmental limitation) |
| Live smoke test #1: `uvicorn` + `POST /chat` for a Claims message matching the knowledge base | `200`, response included `[knowledge=KB-CLAIMS-0002,KB-CLAIMS-0001]`; an unrelated follow-up in the same conversation produced no knowledge annotation |
| Live smoke test #2 (after the `configs/knowledge_base/` relocation, on a fresh `uvicorn` process) | Re-ran the same Claims message — `200`, same `[knowledge=KB-CLAIMS-0002,KB-CLAIMS-0001]` annotation, confirming the relocation did not break the running API |
| Live smoke test: Broker, Commercial Intake, and Fallback routing | All three returned their expected pre-existing responses with no `[knowledge=...]` annotation (no `KnowledgeRetriever` wired to them) — confirms zero regression |
| Manual grep for secrets/keys, prohibited frameworks, and premature Azure AI Search/embeddings implementation across every new/changed RAG file | Clean — all matches are comments/docstrings explicitly stating what is *not* implemented yet (`"No embeddings, no vector database"`, `"a future PBI"`), same pattern as every prior PBI |

Full output (pytest, ruff, mypy, grep, and the real smoke-test transcripts) archived at `docs/sprint_02/evidence/pbi-02-01-knowledge-framework-validation.txt`.

Conclusion: the Knowledge/RAG framework mirrors the Supervisor/Tool/Prompt/LLM frameworks' proven interface-only shape a fifth time; `KnowledgeRetriever` never raises a raw exception (typed `KnowledgeProviderError` only), `LocalKnowledgeProvider` is fully deterministic (no embeddings, no randomness), and every retrieval result carries citation-ready source metadata. `ClaimsAgent` demonstrates real `KnowledgeRetriever` injection without a single line of `src/agents/claims/workflow.py` or `state.py` changing — retrieved knowledge is provably wired into the LLM call (a `[knowledge=...]` annotation) but never touches a business fact, and degrades exactly like a Prompt/LLM failure. Two real, non-Python packaging/repo-hygiene issues were found and fixed during validation (a gitignore collision and a Docker packaging gap), both the same class of issue already documented in prior sprints' decisions — reinforcing that this validation discipline (live smoke test, `git status` review, Docker packaging review) catches real gaps unit tests alone cannot. All 304 tests pass deterministically with no Azure dependency; ruff and mypy clean. No Azure AI Search, embeddings, vector databases, real documents, RAG evaluation, APIM, authentication, or Azure deployment was implemented.

## 2026-08-07 — PBI-02-02: Azure AI Search KnowledgeProvider

| Command | Result |
|---|---|
| Branch topology check (`git log`, `git rev-parse main HEAD`) | Confirmed `feat/pbi-02-02-azure-ai-search-provider` was cut from the already-merged PBI-02-01 tip (`46b29fe`), same commit as `main` — no merge needed |
| `pip show openai azure-identity azure-search-documents azure-core` | `azure-search-documents` was NOT installed (unlike `openai`/`azure-identity`/`azure-core`, already present from the `azureopenai` extra). Installed `azure-search-documents>=11.5,<12` into the dev venv (also added as a new `azuresearch` extra in `pyproject.toml`, never installed by default) |
| `pytest tests/unit/api tests/unit/domain tests/unit/services tests/unit/supervisor tests/unit/agents tests/unit/tools tests/unit/prompts tests/unit/llm tests/unit/rag tests/integration` (immediately after the `dependencies.py` factory-wiring change, before writing `AzureAISearchProvider`) | `304 passed, 2 skipped` — confirms local remains the default with zero behavior change before building the new provider on top of it |
| `pytest tests/unit/rag/test_azure_ai_search_provider.py tests/unit/rag/test_factory.py -v` (new tests only) | `16 passed` — clean on the first run |
| `pytest` full suite | Attempt 1: collection error (`test_factory.py` basename collision between `tests/unit/rag/` and `tests/unit/llm/`, same recurring class of issue as every prior PBI since PBI-00-02). Fixed by renaming to `test_rag_factory.py`. Attempt 2: `320 passed, 2 skipped` |
| `ruff check apps/api/src src tests` | Attempt 1: 1 error (`I001`, unsorted/unformatted import block — the same class of formatting issue every multi-import Azure SDK line in this codebase needs wrapping for). Fixed. Attempt 2: `All checks passed!` |
| `mypy apps/api/src` / `mypy src` | Both clean (13 and 85 files) |
| `mypy tests/unit/llm tests/unit/agents tests/unit/tools tests/unit/prompts tests/unit/supervisor tests/unit/services tests/unit/rag` | Clean (49 files) |
| `az bicep build --file ops/bicep/modules/ai-search.bicep` (new module, standalone, before wiring into `main.bicep`) | exit 0, 0 errors, 0 warnings on the first attempt |
| `az bicep build --file ops/bicep/main.bicep` (after wiring the new module in) | exit 0, 0 errors, 0 warnings |
| `az bicep build` on all 10 modules under `ops/bicep/modules/` (full sweep) | All exit 0, 0 errors, 0 warnings |
| `az bicep build-params` on `dev`/`staging`/`prod` `.bicepparam` (with the new `aiSearchSkuName` param) | All exit 0 |
| Manual grep for secrets/keys, prohibited frameworks across every new/changed file (`src/rag`, `ops/bicep`, `dependencies.py`, `settings.py`, `pyproject.toml`, `.env.example`) | Clean — no matches |
| `docker compose config` (temporary local `.env` copied from `.env.example`, removed after) | exit 0 — unaffected, no Dockerfile changes this PBI (Azure AI Search is not packaged into the image; the SDK is an optional extra, imported lazily) |
| `docker build` | NOT executed — Docker daemon unavailable locally (recurring environmental limitation) |
| Azure deployment (`az deployment group create` or similar) | NOT executed — explicitly out of scope per this PBI's own instructions; only `az bicep build`/`build-params` (offline compilation) were run |
| Live LOCAL smoke test: `uvicorn` + `POST /chat` with no `KNOWLEDGE_PROVIDER` env var set | `200`, Claims response still included `[knowledge=KB-CLAIMS-0002,KB-CLAIMS-0001]` for a relevant message — confirms `KnowledgeSettings` defaults to `local` and the running API never touches Azure AI Search or any Azure SDK |
| Live smoke test: Broker, Commercial Intake, and Fallback routing | All three returned their expected pre-existing responses, confirming zero regression |

Full output (pytest, ruff, mypy, `az bicep build`/`build-params`, grep, and the real smoke-test transcript) archived at `docs/sprint_02/evidence/pbi-02-02-azure-ai-search-provider-validation.txt`.

Conclusion: `AzureAISearchProvider` is a second, fully production-shaped `KnowledgeProvider` implementation, structurally identical in approach to `AzureOpenAIProvider` (lazy SDK imports, Entra ID default, `SecretProvider`-only API-key path, typed exception mapping) and fully mocked in its own 13 dedicated tests — never called against real Azure. Provider selection is entirely configuration-driven (`KNOWLEDGE_PROVIDER`) via a new factory mirroring `src/llm/factory.py` exactly, and local remains the default with proven zero behavior change (the full regression suite was run immediately after the wiring change, before any Azure AI Search code existed). No vector search or semantic ranking was implemented — neither is required by the existing typed contract or justified for this synthetic corpus. A new reusable, RBAC-only Bicep module provisions the Azure AI Search *service* (Free tier for dev, Basic for staging/prod to avoid the one-free-service-per-subscription constraint) with zero index/ingestion/deployment. All 320 tests pass deterministically with no Azure dependency; ruff and mypy clean after one well-understood, justified fix; all 11 Bicep files and all 3 parameter files compile cleanly. No embeddings, vector databases, semantic ranking, RAG index/ingestion pipelines, real documents, citations UI, RAG evaluation, APIM, authentication, or Azure deployment was implemented.

## 2026-08-07 — PBI-02-03: Grounding & Citations Framework

| Command | Result |
|---|---|
| Branch topology check (`git log`, `git rev-parse main HEAD`) | Confirmed no unmerged prior-PBI branch; work proceeded from the already-merged PBI-02-02 tip on `main` |
| `pytest tests/unit/rag -q` (immediately after the additive `KnowledgeMetadata.section`/`source_path` fields, before writing any Grounding code) | `12 passed` (`tests/unit/rag/test_local_provider.py`) — confirms the additive metadata fields are fully backward-compatible before building on top of them |
| `python -c "from src.rag.grounder import Grounder; print('import ok')"` / `mypy src/rag/grounding_models.py src/rag/grounder.py` (immediately after writing the new typed contracts, before integrating into `ClaimsAgent`) | Import succeeded; `Success: no issues found in 2 source files` |
| `pytest tests/unit/supervisor -q` (immediately after extending `AgentResponse` with `citations`/`grounding_metadata`) | `34 passed` — confirms the additive `AgentResponse` fields are backward-compatible |
| `pytest tests/unit/api -q` (immediately after extending `ChatResponse` with `citations`/`groundingMetadata`) | `14 passed` — confirms the additive `ChatResponse` fields are backward-compatible |
| `pytest tests/unit/rag/test_grounder.py tests/unit/rag/test_grounding_models.py -q` (new tests only) | `18 passed` — clean on the first run |
| `pytest tests/unit -q` (full unit suite, after rewiring `ClaimsAgent`'s constructor with `grounder`, updating 5 existing Claims test files, and rewriting `test_claims_agent_knowledge_integration.py`/`test_chat.py`'s bracket-annotation assertions into typed-citation assertions) | `338 passed` — clean on the first run, no basename collision this time (`test_grounder.py`/`test_grounding_models.py` are novel names) |
| `pytest tests/ -q` (full suite incl. `tests/integration`) | `338 passed, 2 skipped` (the 2 skips are pre-existing, unrelated to this PBI) |
| `ruff check src/rag/ src/agents/claims_agent.py src/supervisor/models.py apps/api/src/api/routes/chat.py apps/api/src/api/dependencies.py tests/unit/rag/test_grounding_models.py tests/unit/rag/test_grounder.py tests/unit/agents/ tests/unit/api/test_chat.py` | `All checks passed!` — clean on the first run |
| `mypy src/rag/ src/agents/claims_agent.py src/supervisor/models.py apps/api/src/api/routes/chat.py apps/api/src/api/dependencies.py` | `Success: no issues found in 14 source files` |
| Live smoke test: `uvicorn` + `POST /chat` for a Claims message matching the knowledge base | `200`, response body's `citations` field contained 2 typed citations (`documentId: "KB-CLAIMS-0002"`/`"KB-CLAIMS-0001"`, `referenceId`/`chunkId`/`title`/`score`/`sourcePath` all populated, `section: null`), `groundingMetadata: {"retrievedCount":2,"citationCount":2,"topK":2,"isGrounded":true}` — no `[knowledge=...]` text annotation present anywhere in `response` |
| Live smoke test: `POST /chat` for a Broker message (no `KnowledgeRetriever`/`Grounder` wired) | `200`, `"citations":[],"groundingMetadata":null"` — confirms additive backward compatibility for every non-Claims agent |

Full output (pytest, ruff, mypy, and the real smoke-test transcripts) archived at `docs/sprint_02/evidence/pbi-02-03-grounding-citations-validation.txt`.

Conclusion: the Grounding & Citations layer is a reusable, provider-agnostic extension of the existing Knowledge/RAG framework — `Grounder` imports only `src.rag.models`/`src.rag.grounding_models`, never a concrete `KnowledgeProvider`, so it behaves identically for `LocalKnowledgeProvider` and `AzureAISearchProvider` output. Deduplication, ordering, and top-k capping are all deterministic and independently unit-tested; every public contract (`Citation`, `CitationReference`, `GroundedContext`, `GroundedResponse`, `GroundingMetadata`) is a typed Pydantic model with zero raw dicts or JSON-encoded strings crossing any boundary. "The LLM must never invent a citation" is guaranteed by construction (`Grounder.build_response()` always attaches exactly the citations `GroundedContext` made available), not by post-hoc validation. `ClaimsAgent`'s prior ad-hoc `[knowledge=...]` text annotation was fully replaced by typed fields threaded through `AgentResponse` and `ChatResponse`, both additive and backward-compatible, confirmed by the full pre-existing regression suite plus a live non-Claims smoke test. All 338 tests pass deterministically; ruff and mypy clean on the first run. No vector search, embeddings, semantic ranking, UI rendering, or Azure deployment was implemented.
