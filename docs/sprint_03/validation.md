# Sprint 03 Validation

Record only commands actually executed and their real results.

## 2026-08-07 — PBI-03-01: Ollama LLM Provider and local runtime

| Command | Result |
|---|---|
| Branch topology check (`git log`, `git rev-parse main HEAD`) | Confirmed no unmerged prior-PBI branch; work proceeded from the already-merged PBI-02-04 tip on `main` |
| `python -c "import aiohttp; print(aiohttp.__version__)"` (before writing any Ollama code) | `3.14.3` — confirms `aiohttp` is already installed in the dev venv (transitive Azure-extra dependency), no new install required for local test runs |
| `mypy src/llm/ src/config/` (after `OllamaLLMProvider`, `LLMSettings`, and `factory.py` changes) | `Success: no issues found in 10 source files` — clean on the first run |
| `ruff check src/llm/ src/config/` | `All checks passed!` — clean on the first run |
| `python -c "from src.llm.ollama_provider import OllamaLLMProvider; ..."` (import sanity check) | `import ok` |
| `pytest tests/unit -q` (immediately after the settings/factory/provider changes, before writing any new tests) | `384 passed` — confirms zero regression before adding Ollama-specific tests |
| `pytest tests/unit/llm/test_ollama_provider.py -v` (new tests) | `13 passed` — clean on the first run |
| `ruff check tests/unit/llm/test_ollama_provider.py` | Attempt 1: 2 errors (`PYI034`, `__aenter__` should return `Self` not the concrete class name). Fixed by using `typing.Self`. Attempt 2: `All checks passed!` |
| `mypy tests/unit/llm/test_ollama_provider.py` | `Success: no issues found in 1 source file` |
| `pytest tests/unit/llm/test_factory.py -v` (new `test_factory_returns_ollama_provider_when_configured`) | `4 passed` — clean on the first run |
| `pytest tests/ -q` (full suite) | `398 passed, 2 skipped` — no basename collision this time (`test_ollama_provider.py` is a novel name); the 2 skips are pre-existing, unrelated to this PBI |
| `ruff check apps/api/src src tests` | `All checks passed!` |
| `mypy apps/api/src` / `mypy src` | Both clean (13 and 94 files) |
| `mypy tests/unit/llm tests/unit/agents tests/unit/tools tests/unit/prompts tests/unit/supervisor tests/unit/services tests/unit/rag tests/unit/core` | Clean (56 files). `tests/unit/api/*` excluded for the same pre-existing, unrelated reason as every prior PBI |
| `docker info` | Server unreachable (`failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine`) — Docker Desktop client is installed but the daemon is not running. Same recurring environmental limitation as every prior PBI in this session; not a code defect |
| `docker compose config` (temporary local `.env` copied from `.env.example`, removed after — same technique as every prior PBI) | Structurally valid: exit 0, `LLM_PROVIDER`/`OLLAMA_BASE_URL`/`OLLAMA_MODEL`/`OLLAMA_TIMEOUT_SECONDS` all correctly interpolated into the `api` service's `environment:` block; `extra_hosts: [host.docker.internal=host-gateway]` present |
| `docker compose up` / `docker compose build` | NOT executed — Docker daemon unavailable (see above) |
| `curl --max-time 3 http://localhost:11434/api/tags` | **A real local Ollama server IS running** in this environment (not started by this PBI's work) — `llama3.2:3b` and `llama3.2:1b`, both self-reporting `"capabilities":["completion","tools"]` |
| Live local API smoke test #1: `uvicorn` (default `.env`, `LLM_PROVIDER=mock`) + `POST /chat` for a Claims message | `200`, `"response":"...[llm=mock-llm]"`, `citations`/`groundingMetadata` populated as expected, `"toolCalls":[]` — confirms zero regression to the default (non-Ollama) local runtime |
| Live local API smoke test #2 (real Ollama, not Docker — daemon unavailable): `LLM_PROVIDER=ollama OLLAMA_MODEL=llama3.2:3b uvicorn ...` + `POST /chat` for `"I need to file a claim"` | `200` in ~48.7s. `"response":"Could you provide your policy number? [prompt=claims.system@3.0.0] [llm=llama3.2:3b]"` — proves a REAL Ollama model, not a mock, produced this. `citations`/`groundingMetadata` populated normally (RAG/Grounding unaffected by the provider swap). `toolCalls` contained one entry: `claim_registration` succeeded with `data.claim_reference: "SYN-CLM-2026-0001"` — the model spontaneously requested and received a real Tool execution, end to end through the real `ToolCallingOrchestrator`/`ToolExecutor`. See `decisions.md` for the architectural observation this surfaced (LLM-fabricated Tool arguments, safely isolated from the actual business-fact response text) |
| `taskkill` on both smoke-test `uvicorn` processes | Both stopped cleanly after their respective tests |

Full output (pytest, ruff, mypy, `docker compose config`, and the real smoke-test transcripts) archived at `docs/sprint_03/evidence/pbi-03-01-ollama-provider-validation.txt`.

Conclusion: `OllamaLLMProvider` is a third, fully production-shaped `LLMProvider` implementation, structurally consistent with `AzureOpenAIProvider`'s own proven pattern (lazy SDK import, typed exception mapping, construction-time configuration validation) while reusing an already-declared dependency (`aiohttp`) rather than adding a new one. Provider selection is entirely configuration-driven (`LLM_PROVIDER=ollama`) via one new factory branch, with zero changes required to `apps/api/src/api/dependencies.py`, any Agent, or any other framework — `MockLLMProvider` remains the default for every test and unconfigured local run. Tool Calling support was not only mapped per Ollama's documented API shape but genuinely live-verified in this environment's own running Ollama server, including a real successful Tool execution end to end. `docker-compose.yml` now lets a containerized API reach a host-run Ollama server via `host.docker.internal`, validated structurally via `docker compose config` (the Docker daemon itself was unavailable in this environment — documented as an environmental limitation, not a code defect, consistent with every prior PBI in this session). All 398 tests pass deterministically with no Azure or Ollama dependency; ruff and mypy clean. No Azure deployment, Cosmos production wiring, Azure networking, APIM, authentication, new agents, or new RAG features were implemented.

## 2026-08-07 — PBI-03-02: Azure Runtime integration

| Command | Result |
|---|---|
| Branch topology check (`git log`, `git rev-parse main HEAD`) | Confirmed no unmerged prior-PBI branch; work proceeded from the already-merged PBI-03-01 tip on `main` |
| Inspection pass (`AzureOpenAIProvider`, `AzureAISearchProvider`, `CosmosConversationRepository`, `SecretProvider`/Key Vault, Managed Identity, all Bicep modules, `apiContainerApp`'s `env` list, composition root) | Found the real gap: Cosmos DB and Azure AI Search were already provisioned by prior sprints, but `apiContainerApp`'s `env` list never set `LLM_PROVIDER`/`KNOWLEDGE_PROVIDER`/`CONVERSATION_STORE_PROVIDER` or any Azure endpoint at all — a deployed container would silently fall back to Mock/local/in-memory. No `azure-openai.bicep` module existed (explicitly out of scope in Sprint 0). This drove the entire PBI-03-02 scope |
| `az bicep build --file ops/bicep/modules/azure-openai.bicep` (new module, standalone, before wiring into `main.bicep`) | exit 0, 0 errors, 0 warnings on the first attempt |
| `az bicep build --file ops/bicep/main.bicep` (after wiring the new module, new params, and the extended `apiContainerApp` env list) | exit 0, 0 errors, 0 warnings on the first attempt |
| `az bicep build` on all 11 modules under `ops/bicep/modules/` (full sweep) | All exit 0, 0 errors, 0 warnings |
| `az bicep build-params` on `dev`/`staging`/`prod` `.bicepparam` (with the new `llmProvider`/`knowledgeProvider`/`conversationStoreProvider`/`azureOpenAi*`/`aiSearchIndexName` params) | All exit 0 |
| `pytest tests/unit/services/test_conversation_store_factory.py -v` (new) | `4 passed` — clean on the first run |
| `pytest tests/unit/llm/test_factory.py tests/unit/rag/test_rag_factory.py -v` (new missing-endpoint-via-factory tests added) | `9 passed` — clean on the first run |
| `pytest tests/unit/api/test_dependencies.py -v` (new composition-root tests, 13 tests covering LLM/Knowledge/Cosmos provider selection, missing-configuration failures, Managed-Identity-default auth path, and full three-provider composition) | `13 passed` — clean on the first run |
| `pytest tests/unit -q` (full unit suite, immediately after adding the new composition-root tests, checking for `@lru_cache` cross-test pollution) | `417 passed` — the autouse cache-clearing fixture in `test_dependencies.py` left zero pollution; every other test (including `test_chat.py`, which builds `main.app`/`get_supervisor()` via `TestClient`) still saw the Mock/local/in-memory defaults |
| `ruff check tests/unit/api/test_dependencies.py tests/unit/services/test_conversation_store_factory.py tests/unit/llm/test_factory.py tests/unit/rag/test_rag_factory.py` | Attempt 1: 8 errors (`RUF100`, unused `# noqa: SLF001` comments — `SLF001` is not enabled in this project's ruff config). Fixed via `ruff check --fix`. Attempt 2: `All checks passed!` |
| `mypy tests/unit/api/test_dependencies.py` | 1 error: `Cannot find implementation or library stub for module named "api"` — confirmed via `mypy tests/unit/api/test_chat.py` to be the exact same pre-existing, unrelated `sys.path`-injection limitation every `tests/unit/api/*` file already has (not a regression) |
| `mypy tests/unit/services/test_conversation_store_factory.py tests/unit/llm/test_factory.py tests/unit/rag/test_rag_factory.py` | `Success: no issues found in 3 source files` |
| `pytest tests/ -q` (full suite, re-run after all fixes) | `417 passed, 2 skipped` — the 2 skips are pre-existing, unrelated to this PBI |
| `ruff check apps/api/src src tests` | `All checks passed!` |
| `mypy apps/api/src` / `mypy src` | Both clean (13 and 94 files) |
| `mypy tests/unit/llm tests/unit/agents tests/unit/tools tests/unit/prompts tests/unit/supervisor tests/unit/services tests/unit/rag tests/unit/core` | Clean (57 files). `tests/unit/api/*` excluded for the same pre-existing, unrelated reason as every prior PBI |
| `az bicep build` full re-sweep (`main.bicep` + all 11 modules, final validation pass) | All exit 0, 0 errors, 0 warnings |
| `az bicep build-params` full re-sweep (dev/staging/prod, final validation pass) | All exit 0 |
| `docker compose config` (temporary local `.env` copied from `.env.example`, removed after — same technique as every prior PBI; `docker-compose.yml` itself untouched by this PBI) | exit 0 |
| `docker info` | Server unreachable — same recurring environmental limitation as PBI-03-01 and every infra-touching PBI this session; not a code defect. No `az deployment ... create`/`what-if`, `docker compose up`, or `docker compose build` executed — none were in scope for this PBI regardless |
| Live local smoke test: `uvicorn` (default `.env`, unset `LLM_PROVIDER`/`KNOWLEDGE_PROVIDER`/`CONVERSATION_STORE_PROVIDER`) + `POST /chat` for a Broker message | `200`, `"response":"...[llm=mock-llm]"`, `"citations":[]`, `"groundingMetadata":null`, `"toolCalls":[]` — confirms the local (Mock/local/in-memory) runtime remains completely unaffected by all the Azure Bicep/composition-root wiring added this PBI |
| `taskkill` on the smoke-test `uvicorn` process | Stopped cleanly after the test |

Full output (pytest, ruff, mypy, `az bicep build`/`build-params`, `docker compose config`, and the smoke-test transcript) archived at `docs/sprint_03/evidence/pbi-03-02-azure-runtime-integration-validation.txt`.

Conclusion: PBI-03-02 closed the real gap this PBI's own inspection found — Cosmos DB and Azure AI Search were already fully provisioned by prior sprints but never actually selected by the deployed API Container App, because no provider-selection or endpoint env vars were ever set on it. That gap is now closed entirely through configuration and completed IaC, with zero duplication of any existing Azure provider implementation and zero Agent changes. The new `azure-openai.bicep` module completes the Bicep inventory CLAUDE.md §4 requires, with least-privilege RBAC (Cognitive Services OpenAI User) matching the exact pattern already proven for Cosmos DB/AI Search/Key Vault/ACR. `knowledgeProvider` deliberately stays `local` — not `azure_ai_search` — in every parameter file, a considered safety decision (not an oversight) documented in `decisions.md`, since no AI Search index exists yet and defaulting to Azure there would ship a container that fails at startup. New composition-root tests exercise the real `apps/api/src/api/dependencies.py` (not just the lower-level factories), including a full three-provider Azure composition test and explicit verification that Managed Identity remains the default auth path with no `SecretProvider` ever built unless an app explicitly opts into key auth. All 417 tests pass deterministically; ruff and mypy clean; all 12 Bicep files and all 3 parameter files compile cleanly; the local (non-Azure) runtime is provably unaffected. No Azure resources were deployed, no real Azure credentials were used anywhere, and the current networking posture (public network access + RBAC-only) is now formally documented — as its own first ADR in this repository — against what a future, separately-scoped hardening PBI would still need to do (VNet integration, Private Endpoints), exactly as instructed.

## 2026-08-07 — PBI-03-03: Azure AI Search Index & Knowledge Ingestion Pipeline

| Command | Result |
|---|---|
| Branch topology check (`git log`, `git rev-parse main HEAD`) | Confirmed no unmerged prior-PBI branch; work proceeded from the already-merged PBI-03-02 tip on `main` |
| Inspection pass (`AzureAISearchProvider`, Knowledge Framework, Grounding, `LocalKnowledgeProvider`, Bicep) | Confirmed `src/pipelines/` is CLAUDE.md §6-reserved and still empty (`.gitkeep` only) — the correct home for this work; confirmed `AzureAISearchProvider._SELECT_FIELDS` deliberately excluded `section`/`source_path` in PBI-02-02 pending "a future PBI's index" — this PBI is that PBI; confirmed no PDF-parsing library (`pypdf`, `pdfplumber`) is installed anywhere in the project |
| `python -c "from azure.search.documents.indexes.aio import SearchIndexClient; from azure.search.documents.indexes.models import SearchIndex, SimpleField, SearchableField, SearchFieldDataType; ..."` | `ok` — confirms the index-schema SDK surface is already available via the existing `azure-search-documents` dependency, no new dependency needed |
| `mypy src/pipelines/` (incrementally, after each new module: exceptions, models, embedding, index_schema, loaders, pipeline) | `Success: no issues found` at every step — clean on the first attempt for every file |
| `ruff check src/pipelines/` (same incremental cadence) | `All checks passed!` at every step |
| Manual sanity check: `MarkdownDocumentLoader().load(path)` against all 5 real `configs/knowledge_base/*.md` files | All 5 parsed to exactly 1 chunk each (`chunk_id` == `source_id`), matching `LocalKnowledgeProvider`'s existing one-chunk-per-document behavior exactly — confirms zero regression risk to the real corpus before writing any tests |
| Manual sanity check: `MarkdownDocumentLoader().load(path)` against a synthetic 3-section document | Correctly produced 1 unsectioned lead-in chunk + 2 section chunks, each with a stable, slug-derived `chunk_id`, correct `section` text, and a distinct `content_hash` per chunk |
| `pytest tests/unit/pipelines -v` (new tests, all 5 files) | `32 passed` — clean on the first run |
| `ruff check tests/unit/pipelines/` | `All checks passed!` |
| `mypy tests/unit/pipelines/` | `Success: no issues found in 5 source files` |
| `pytest tests/unit/rag/test_azure_ai_search_provider.py -v` (after extending `_SELECT_FIELDS` and adding 2 new tests) | `15 passed` — clean on the first run, zero regression to the 13 pre-existing tests |
| `ruff check src/rag/azure_ai_search_provider.py tests/unit/rag/test_azure_ai_search_provider.py` | `All checks passed!` |
| `mypy src/rag/azure_ai_search_provider.py tests/unit/rag/test_azure_ai_search_provider.py` | `Success: no issues found in 2 source files` |
| `ruff check ops/scripts/ingest_knowledge_base.py` | Attempt 1: 1 error (`I001`, unsorted import block). Fixed via `ruff check --fix`. Attempt 2: `All checks passed!` |
| `mypy ops/scripts/ingest_knowledge_base.py` | Attempt 1: 2 errors (`_build_credential`'s return type `AzureKeyCredential \| object` too loose for `SearchIndexClient`/`SearchClient`'s `credential` parameter). Fixed by typing it `AzureKeyCredential \| AsyncTokenCredential`. Attempt 2: `Success: no issues found in 1 source file` |
| Import sanity check: load `ops/scripts/ingest_knowledge_base.py` as a module (not executed as `__main__`) | `import ok` |
| `pytest tests/ -q` (full suite) | `451 passed, 2 skipped` — no basename collision (every new test file uses a unique `test_ingestion_*.py` prefix, chosen proactively after confirming zero existing duplicate basenames repo-wide); the 2 skips are pre-existing, unrelated to this PBI |
| `ruff check apps/api/src src tests ops/scripts` | `All checks passed!` |
| `mypy apps/api/src` / `mypy src` | Both clean (13 and 102 files) |
| `mypy tests/unit/llm tests/unit/agents tests/unit/tools tests/unit/prompts tests/unit/supervisor tests/unit/services tests/unit/rag tests/unit/core tests/unit/pipelines` | Clean (62 files). `tests/unit/api/*` excluded for the same pre-existing, unrelated reason as every prior PBI |
| `az bicep build` on `main.bicep` + all 11 modules (regression check — no Bicep changes made this PBI) | All exit 0, 0 errors, 0 warnings |
| `docker compose config` (temporary local `.env` copied from `.env.example`, removed after — same technique as every prior PBI; regression check — `docker-compose.yml` untouched this PBI) | exit 0 |

Full output (pytest, ruff, mypy, `az bicep build`, `docker compose config`) archived at `docs/sprint_03/evidence/pbi-03-03-knowledge-ingestion-pipeline-validation.txt`.

Conclusion: PBI-03-03 completes the Azure Knowledge platform this sprint has been building toward — `AzureAISearchProvider` (PBI-02-02) has had no index to query and no way to populate one since it was built; that gap is now closed by a real, tested ingestion pipeline living in CLAUDE.md §6's reserved `src/pipelines/` folder. The index schema is defined exactly once, in Python, shared by both the ingestion pipeline (what gets written) and `AzureAISearchProvider` (what gets read back via its now-extended `_SELECT_FIELDS`) — eliminating the risk of two competing schema definitions drifting apart, a risk that would have existed had the schema also been duplicated in Bicep. Markdown ingestion is fully real, including new section-based chunking that makes Grounding's `section` field genuinely populated for the first time; incremental ingestion and delete/update handling are genuine, content-hash-diffed behaviors, not stubs, each independently unit-tested against a mocked Azure AI Search client. PDF ingestion, real embeddings, and SharePoint integration are all deliberately shipped as wired, honest abstractions rather than speculative implementations — consistent with CLAUDE.md §7's "do not add dependencies unless explicitly required" and every prior RAG PBI's "no vector search" exclusion. Zero Agent, Supervisor, Prompt, or Tool code was touched. All 451 tests pass deterministically; ruff and mypy clean; all Bicep files re-validated with zero regression (no Bicep changes were needed — index management belongs in Python, not IaC). No Azure resources were deployed and the new `ingest_knowledge_base.py` CLI was never executed against real Azure.
