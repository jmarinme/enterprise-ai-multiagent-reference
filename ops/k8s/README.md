# Kubernetes reservation

This folder is retained because it is part of the academic repository template.

The MVP uses **Azure Container Apps**, not AKS. Do not add Kubernetes manifests unless
a future ADR demonstrates a requirement that Container Apps cannot satisfy.
